<h1>Editing Friend</h1>

<%= render 'form', friend: @friend %>
<br>
<div>
<%= link_to 'Show', @friend, class: "btn btn-secondary btn-sm" %> |
<%= link_to 'Back', friends_path, class: "btn btn-secondary btn-sm"%> |  <%= link_to 'Destroy', @friend, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %>
</div>

<!-- wala dito yung update button because the form is called in different pages. the button update will just change its name from create to update-->